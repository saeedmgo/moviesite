import React, { useEffect, useState } from "react";
import { useParams } from "react-router";
export default function ineerPage() {
  const [posts, setposts] = useState([]);
  useEffect(() => {
    fetch(``);
  }, []);

  return <div></div>;
}
